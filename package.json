{
  "name": "ascii-defense",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Roguelite tower defense rendered entirely in ASCII",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "vite packages/app",
    "typecheck": "tsc --build",
    "build": "npm run typecheck && vite build packages/app",
    "preview": "vite preview packages/app"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
